services:
  app:
    build: .
    container_name: gk_qna_app
    ports:
      - "8011:8011"
    env_file:
      - .env
    environment:
      - TZ=Asia/Kolkata
    restart: unless-stopped
    volumes:
      - .:/code/app
